services:
  mcp:
    build: .
    restart: unless-stopped
    expose:
      - "8000"

  edge:
    image: nginx:stable
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - mcp
